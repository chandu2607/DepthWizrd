# --- core (required for baselines A/B, metrics, report) ---
numpy>=1.23
opencv-python-headless>=4.7
pyyaml>=6.0
matplotlib>=3.6

# --- learned head (Baseline C) + Depth Anything V2 prior ---
# On Colab/Kaggle these are preinstalled or one pip away. CPU-only works but is slow.
torch>=2.0
transformers>=4.40
huggingface_hub>=0.23
Pillow>=9.0

# --- geospatial / raster IO (real DFC2019 GeoTIFFs) ---
# rasterio is preferred (handles nodata + CRS); tifffile is a lighter fallback.
# rasterio wheels can be finicky on Windows -- tifffile alone is enough to READ
# the DFC2019 tiles for Phase-1 (no reprojection needed yet).
tifffile>=2023.1
rasterio>=1.3 ; platform_system != "Windows"

# --- tests (optional) ---
pytest>=7.0
